{
    "name": "phpnitro/ui",
    "description": "PhpNitro widget SDK — PHP classes that render to Tailwind-styled HTML, served by a real PHP runtime on-device.",
    "type": "library",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^13.2"
    },
    "autoload": {
        "psr-4": {
            "Engine\\": "src/"
        }
    }
}

{
    "name": "phpnitro/ui",
    "description": "PhpNitro's UI package — the native render engine widgets (Engine\\Native\\*: layout, Canvas draw commands, animations) and core drawing primitives.",
    "license": "MIT",
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^13.2"
    },
    "autoload": {
        "psr-4": {
            "Engine\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Engine\\Tests\\": "tests/"
        }
    }
}
